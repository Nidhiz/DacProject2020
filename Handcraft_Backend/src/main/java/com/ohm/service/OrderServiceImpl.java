package com.ohm.service;

/*

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;


import com.ohm.pojos.Order;
import com.ohm.repository.IOrderRepository;

@Service
@Transactional
public class OrderServiceImpl implements IOrderService {
	@Autowired
	private IOrderRepository orderRepo;
	

	@Override
	public Order saveOrUpdateAOrder(Order order) {
		return orderRepo.save(order);
	}

	@Override
	public Order findByOrderId(Long orderId) {
		
		return orderRepo.findByOrderId(orderId);
	}
	@Override
	public Order addOrderDetails(Order orderTransientPojo)
	{
		System.out.println(orderTransientPojo.getOrderDetails());
		return orderRepo.save(orderTransientPojo);
	}
	@Override
	public Iterable<Order> findAll() {
		return orderRepo.findAll();
	}

}*/
